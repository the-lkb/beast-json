version: 2
updates:
  # Keep GitHub Actions up to date
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # Keep docs npm dependencies up to date (auto-updates package-lock.json)
  - package-ecosystem: "npm"
    directory: "/docs"
    schedule:
      interval: "weekly"
